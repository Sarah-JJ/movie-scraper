const HOST_NAME = {
    IMDB: 'www.imdb.com',
    YOUTUBE: 'www.youtube.com/'
};

const CLI_OPTIONS = {
    MOVIES: 'movies',
    SWAGGER_SECTIONS: 'swaggerExampleSections'
};

module.exports = {HOST_NAME, CLI_OPTIONS};
